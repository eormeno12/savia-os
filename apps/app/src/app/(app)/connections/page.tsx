import { SpaceControlPanel } from "@/components/connections/SpaceControlPanel";

export const metadata = { title: "Conexiones — Savia" };

export default function ConnectionsPage() {
  return <SpaceControlPanel />;
}
