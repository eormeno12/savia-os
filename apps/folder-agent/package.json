{
  "name": "@savia-os/folder-agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "El agente del canal `folder`: observa una carpeta, hashea y reporta. Nucleo en Rust (src-tauri), cara en webview, y un servidor simulado para construirlo antes de que Savia tenga API.",
  "scripts": {
    "sim": "node sim/server.ts",
    "sim:ejercicio": "node sim/ejercicio.ts",
    "nucleo:fmt": "cargo fmt --manifest-path src-tauri/Cargo.toml --check",
    "nucleo:clippy": "cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings",
    "nucleo:windows": "cargo check --manifest-path src-tauri/Cargo.toml --all-targets --target x86_64-pc-windows-msvc",
    "nucleo:test": "cargo test --manifest-path src-tauri/Cargo.toml",
    "nucleo:banco": "node sim/banco.ts",
    "nucleo:build": "cargo build --manifest-path src-tauri/Cargo.toml",
    "nucleo:lint": "pnpm nucleo:fmt && pnpm nucleo:clippy && pnpm nucleo:windows",
    "lint": "pnpm nucleo:lint && pnpm nucleo:test && pnpm sim:ejercicio && pnpm nucleo:banco && pnpm panel:contraste",
    "panel:contraste": "node scripts/contraste.mjs"
  }
}
